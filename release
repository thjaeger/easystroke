#!/bin/bash
VERSION=`git describe`
DIST=easystroke-$VERSION
TGZ=$DIST.tar.gz

whiptail --yesno "create and upload $TGZ?" 7 70 || exit 0
git archive --format=tar --prefix=$DIST/ $1 | gzip > $TGZ

rsync -avP $TGZ thjaeger@frs.sourceforge.net:uploads/
rm $TGZ
